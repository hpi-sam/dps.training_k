export class Device {
    /**
     * @param {String} name
     * @param {String} deviceDatabaseId
     */
    constructor(name, deviceDatabaseId) {
        this.name = name;
        this.deviceDatabaseId = deviceDatabaseId;
    }
}